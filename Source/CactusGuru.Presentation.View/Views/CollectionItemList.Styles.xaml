<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:CactusGuru.Presentation.View.Tools.Controls"
                    xmlns:converters="clr-namespace:CactusGuru.Presentation.View.Tools.Converters">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/CactusGuru.Presentation.View;component/Themes/Generic.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    <converters:StringVisibilityConverter x:Key="stringVisConverter"/>
    <Style TargetType="controls:SimpleButton">
        <Setter Property="Padding" Value="10"/>
        <Setter Property="Margin" Value="1"/>
        <Setter Property="Background" Value="#f0f8fd"/>
        <Setter Property="OnOverBackground" Value="White"/>
    </Style>
    <Style TargetType="ListBox" BasedOn="{StaticResource ListBoxStyle}">
        <Setter Property="BorderThickness" Value="0 1 0 0"/>
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Grid Height="35">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.RowSpan="2" Text="{Binding Code}" FontSize="14" VerticalAlignment="Center" FontWeight="Bold" Margin="0 0 8 0"/>
                        <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="13" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding Info}" Visibility="{Binding Info, Converter={StaticResource stringVisConverter}}" FontSize="11"/>
                    </Grid>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ContextMenu">
            <Setter.Value>
                <ContextMenu>
                    <ContextMenu.Template>
                        <ControlTemplate>
                            <Border Padding="1" Background="#cde7f8" Width="130">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <controls:SimpleButton Command="{Binding CopyNameCommand}" Height="60">
                                        <StackPanel>
                                            <Image Source="/CactusGuru.Presentation.View;component/Resources/Images/Icon/CollectionList/copy_20.png" Width="20" Height="20"/>
                                            <TextBlock>کپی</TextBlock>
                                        </StackPanel>
                                    </controls:SimpleButton>
                                    <controls:SimpleButton Grid.Column="1" Command="{Binding EditCurrentCollectionItemCommand}" Height="60">
                                        <StackPanel>
                                            <Image Source="/CactusGuru.Presentation.View;component/Resources/Images/Icon/EditorViewBasics/edit_24.png" Stretch="None"/>
                                            <TextBlock>ویرایش</TextBlock>
                                        </StackPanel>
                                    </controls:SimpleButton>
                                    <controls:SimpleButton Grid.Row="1" Grid.ColumnSpan="2" Height="40" Command="{Binding DeleteCurrentCollectionItemCommand}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock VerticalAlignment="Center">حذف</TextBlock>
                                            <Image VerticalAlignment="Center" Source="/CactusGuru.Presentation.View;component/Resources/Images/Icon/CollectionList/delete_24.png" Width="24" Height="24"/>
                                        </StackPanel>
                                    </controls:SimpleButton>
                                    <controls:SimpleButton Grid.Row="2" Grid.ColumnSpan="2" Height="40" Command="{Binding GotoImageGallaryCommand}">گالری</controls:SimpleButton>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </ContextMenu.Template>
                </ContextMenu>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="ListBoxItem" BasedOn="{StaticResource ListBoxItemStyle}"/>
</ResourceDictionary>